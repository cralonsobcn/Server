services:
  codeServer:
    image: lscr.io/linuxserver/code-server:4.106.3-ls308
    container_name: code-server
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
      SUDO_PASSWORD: "test"
    networks:
      - cluster-network
    volumes: # TODO 
      - ${HOME}/Server/code-server/config:/config
    restart: ${RESTART_POLICY}