services:
  web-check: 
    image: docker.io/lissy93/web-check:1.0.0
    container_name: web-check
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
    networks:
      - cluster-network
    restart: ${RESTART_POLICY}