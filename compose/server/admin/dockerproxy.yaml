services:
  server-dockerproxy: # Service to securly expose the docker socket to the HOMEPAGE app in Server
    image: ghcr.io/tecnativa/docker-socket-proxy:latest
    container_name: server-dockerproxy
    environment:
      - CONTAINERS=1 # Allow access to viewing containers
      - SERVICES=1 # Allow access to viewing services (necessary when using Docker Swarm)
      - TASKS=1 # Allow access to viewing tasks (necessary when using Docker Swarm)
      - POST=0 # Disallow any POST operations (effectively read-only)
      - INFO=1 # Necessary to query logs from Dazzle/Server
      - CONTAINERS=1 # Necessary to query logs from Dazzle/Server
      - ALLOW_START=1 # Necessary to run whatsupdocker 
      - ALLOW_STOP=1 # Necessary to run whatsupdocker 
      - IMAGES=1 # Necessary to run whatsupdocker
      - LOG_LEVEL=err # Set log level to error to reduce log verbosity
    networks:
      - server_network
      - server_tunnel_network
    ports:
      - ${SERVER_IP}:2380:2375 # 2375 Already in use??
    volumes:
      - ${DOCKER_SOCK}:${DOCKER_SOCK}:ro # Mounted as read-only
    restart: ${RESTART_POLICY}