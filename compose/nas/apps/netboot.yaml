services:
  netbootxyz:
    image: lscr.io/linuxserver/netbootxyz:latest
    container_name: netbootxyz
    environment:
      # You can set any environment variable from a file by using a special prepend FILE__.
      # -e FILE__MYVAR=/run/secrets/mysecretvariable
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
      MENU_VERSION: 2.0.84 #optional
      PORT_RANGE: 30000:30010 #optional. Specify the port range tftp will use for data transfers (see Wikipedia)
      SUBFOLDER: / #optional
      NGINX_PORT: 80 #optional
      WEB_APP_PORT: 3000 #optional
    networks:
      - nas_network
    ports:
      - ${NETBOOT_HOST_TCP_PORT}:${NETBOOT_CONTAINER_TCP_PORT}/tcp
      - ${NETBOOT_HOST_UDP_PORT}:${NETBOOT_CONTAINER_UDP_PORT}/udp
      # - 8080:80 #optional
    volumes: # TODO move to volume
      - ${HOME}/NAS/netbootxyz/config:/config # Storage for boot menu files and web application config
      - ${HOME}/NAS/netbootxyz/assets:/assets # optional # Storage for NETBOOT.XYZ bootable assets (live CDs and other files)
    restart: ${RESTART_POLICY}